import { EventConstraintsPipe } from './event-constraints.pipe';

describe('EventConstraintsPipe', () => {
  it('create an instance', () => {
    const pipe = new EventConstraintsPipe();
    expect(pipe).toBeTruthy();
  });
});
